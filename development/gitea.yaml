path: https://gitea.com/api/v1/users/%s
mode: JSON
category: development
timeout: 5
headers:
  Accept: application/json
selectors:
  id: id
  username: login
  name: full_name
  email: email
  avatar: avatar_url
  profile: html_url
  created: created
  location: location
  website: website
